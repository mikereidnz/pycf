Python fixes

  1. njsymbols.py — wigner_3j half-integer summation bounds

  np.arange(xmin, xmax+1) steps by 1, so with a half-integer xmax=0.5 it generates [0.0, 1.0] — including an out-of-range x=1 that produces factorial(-0.5)=0, causing
  divide-by-zero. Fix: clamp bounds to integers.

   # line 99-101 — replace
   xmin = max([0, j3 - j2 - m1])
   xmax = min([j3 + m3, j1 - m1, j3 + j2 - m1])
   xlist = np.arange(xmin, xmax + 1)
   # with
   xmin = int(np.ceil(max([0, j3 - j2 - m1])))
   xmax = int(np.floor(min([j3 + m3, j1 - m1, j3 + j2 - m1])))
   xlist = np.arange(xmin, xmax + 1)

  2. inten.py — boltzmann_factor at T=0

  Returns 1 for every level at T=0; excited states should have weight 0.

   elif t == 0:
       ans = 1 if e == 0 else 0   # only ground state populated

  3. inten.py — dipole_str docstring

  Docstring says lrange = [i_first, i_last, f_first, f_last] but code does for i in lrange[0] (expects iterables). Correct the docstring:

   lrange : list of two iterables
       [initial_levels, final_levels] — each element is a range or list of
       0-based level indices (e.g. [range(0, 4), range(4, 8)]).

  4. cfl_util.py — rotate_cf_params zero-sum guard

  if np.sum(p) != 0 passes even when terms cancel (e.g. {C20: 2, C22: -1} has sum 1, but other combos can sum to 0 while containing nonzero parameters). Replace with a genuine
  zero-check:

   if np.any(np.array(p) != 0):

  5. pyemp.py line 561 — numpy array truth value

  if Iz != 0: raises ValueError when Iz is a numpy array.

   if not np.all(Iz == 0):

  6. cfl.pyx sh_hpro_helper — coeff_dict alias mutation

   tmp_h_coeff = h.coeff_dict       # alias — mutation below corrupts caller's dict
   tmp_h_coeff['MAGZS'] = 1

  Fix: copy first:

   tmp_h_coeff = dict(h.coeff_dict)

 ~/dev/pycf [⎇ mfr-upgrade-python*+%]     
 
   ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

  C test fixes

  7–9. Remove stale chisq_weight on ex_data

  This field moved to shx_data. Delete in:

   - nls_test.c:176
   - siman_test.c:186
   - opt_test.c:509

  10–12. Add missing kramers arg to zsh_alloc

  Current signature: zsh_alloc(inter, ninter, sz, iz, **kramers**, **a). All three calls below are for Kramers ions (kramers=1):

   - opt_test.c:575: zsh_alloc(inter, 1, 1, 0, 1, inv_a)
   - sh_test.c:638: zsh_alloc(sls_inter, 1, 1, 1, 1, sls_inv_a)
   - sh_test.c:723: zsh_alloc(inter, 2, 1, 1, 1, inv_a)

  13. sh_test.c:643 — remove call to deleted zshp_gen_sort

  zshp_gen_sort no longer exists in the API (removed, not renamed). The call should be deleted; the surrounding alloc/free cycle still exercises zshp_p_w_alloc and zsh_alloc.

  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
21/4/16 8am
● Deep audit complete enough to act on. The repo still builds and tests cleanly, but I found 14+ actionable bugs/consistency issues, including 2 critical Python fit-statistics
  bugs, 8 confirmed C/runtime bugs, and several test/example problems. The only incomplete part is that one Python sub-agent hit rate limits, but the important Python findings
  were manually confirmed.

  Highest priority
  
  [Note that there is also an output bug for % of state in summary. Need to fix this. ]

   1. pycf/cfl.pyx:3381,3449,3531,3607 and pycf/cfl_util.py:614 compute ndof backwards as n_p_real - n_obs, so it gets clamped to 1 almost always.
   2. pycf/cfl_util.py:340,487,562 computes sigma as sqrt(chi2/weighting) / ndof; ndof should be inside the square root.
   3. cfl/src/cfl_csr.c:954-958 has a use-after-free on pm->val.
   4. cfl/src/cfl_csr.c:92-112 has broken CSR row_ptr sentinel logic for leading zero rows.
   5. cfl/src/cfl_csr.c:503-507 frees uninitialized hcsr_m.
   6. cfl/src/cfl_zefoz.c:54-67,308-348 uses cabs in ZEFOZ derivatives, losing sign/phase information.
   7. cfl/src/cfl_h.c:91-95 leaks on tensor-label mismatch.
   8. cfl/src/cfl_sh.c:121-139 leaks on invalid interaction type.
   9. cfl/src/cfl_sh.c:152-163 returns zsh with slabels uninitialized.
   10. cfl/src/cfl_sh.c:138,598-600,681-682 likely undersizes the LAPACK b buffer.

  Tests/examples

   - tests/spin-half/spin-half_test.py:81-83 — "complex" case is identical to "imag", so the true complex path is untested.
   - tests/ce_examples/exdata_test.py — all three parametrized datasets share one expected_coeff block, and the tolerance is loose enough to mask real differences.
   - examples/eryso/mesh_fit.py:175-176 — writes output into the tracked sample file 2017-05-17_eryso_site1.txt.
   - examples/eryso/mesh_fit.py:49,59 and examples/eryso/mhfit_siman.py:59,67 — duplicate ZETA keys silently discard the first value.
   - examples/ceylf/exdata_example.py:85 — error message says sel_data instead of data_sel.

  Lower-priority consistency issues

   - tests/spin-half/spin-half_test.py constructs mutable globals at import time.
   - examples/Altp_ercawo4/ercawo4_altp_calc.py looks stale against newer pymatgen neighbor APIs.
   - examples/eryso/mcmc_analysis.py:65 uses deprecated set_yticklabels([]).
   - examples/eryso/mhfit_siman.py has a duplicate 't' key and dead hf_pd entries.

  Runtime notes

   - python -m pytest -q: pass.
   - make -C cfl test: pass.
   - Shipped SLJM datasets load.
   - Most example scripts run when started from their own directories with PYTHONPATH set.
   - examples/smnacaf2/smnacaf2.py did not finish within a 600s guard, but it also did not crash; it looks computationally heavy rather than immediately broken.



Numbered issues above were fixed. I will fix the tests myself later. 

----------------- 21/4/26 12.56


  Full Repo Scan Results

  🔴 Critical — Cython error-path double-frees (cfl.pyx)

  Three classes have the same pattern: a C pointer is freed in an __init__ error path but self.<field> is not set to NULL, so __dealloc__ frees it a
  second time when the exception propagates.

  ┌────┬─────────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │ #  │ Location                        │ What gets double-freed                                                                                   │
  ├────┼─────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ P1 │ Hamiltonian.__cinit__ (~line    │ self.tensor_array freed at line 337, then again in __dealloc__ at line 347                               │
  │    │ 330/337/347)                    │                                                                                                          │
  ├────┼─────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ P2 │ EFit.__init__ (~line 1557)      │ self.ex_data freed on param_array alloc failure, then again in __dealloc__ at line 1603                  │
  ├────┼─────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ P3 │ EFit.__init__ (~line 1567)      │ free(self.param_array) is called when self.param_array is still NULL (assignment is line 1582) — leaks   │
  │    │                                 │ the param_array allocation instead                                                                       │
  ├────┼─────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ P4 │ ESHFit.__init__ (~line          │ self.param_array assigned early (line 2215), freed in inner error loop (line 2225), then elements +      │
  │    │ 2215/2225)                      │ outer freed again in __dealloc__                                                                         │
  └────┴─────────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  -----------
    🔴 Critical — C integer overflow in calloc size arguments

  int * int multiplied before passing to calloc; overflows silently for large dimensions (harmless for typical CF problem sizes, but technically UB):

  ┌───────────────┬──────────┬─────────────────────────────┐
  │ File          │ Line     │ Expression                  │
  ├───────────────┼──────────┼─────────────────────────────┤
  │ cfl_h.c       │ 495, 508 │ block_dim[i]*block_dim[i]   │
  ├───────────────┼──────────┼─────────────────────────────┤
  │ cfl_h_fit.c   │ 126      │ h->n*h->n                   │
  ├───────────────┼──────────┼─────────────────────────────┤
  │ cfl_zefoz.c   │ 175      │ h->n*h->n                   │
  └───────────────┴──────────┴─────────────────────────────┘

  Fix: cast to (size_t) before the multiply.

-------------------------------------------------------------------------------------------------------------------------------------------------

  🟡 Medium — Python/C

  ┌────┬───────────────────────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │ #  │ File                      │ Issue                                                                                                          │
  ├────┼───────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ C1 │ cfl_sh.c:79,256           │ int i loop variable vs size_t ninter upper bound — signed/unsigned comparison                                  │
  ├────┼───────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ C2 │ cfl_zefoz.c:119–130       │ After two sequential reallocs, if the second fails the first is committed but not rolled back (partial resize) │
  ├────┼───────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ P5 │ import_sljm.py:51,88      │ Regex for STATES count and state-label lines can fail silently; both return empty lists without raising an     │
  │    │                           │ error                                                                                                          │
  ├────┼───────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ T1 │ spin-half_test.py:54–83   │ Module-level coeff dict mutated inside each parametrised variant — creates test-order dependency               │
  └────┴───────────────────────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  ------------------------------------

  🟢 Low — cosmetic / known

  ┌─────┬───────────────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────┐
  │ #   │ File                                  │ Issue                                                                             │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L1  │ inten.py:62,183,230                   │ Invalid Python escape sequences (\lambda, etc.) in docstrings → SyntaxWarning     │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L2  │ spinh.py:346                          │ Same escape-sequence issue in docstring                                           │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L3  │ spin-half_test.py:66,80–83            │ "complex" parametrize variant identical to "imag" (already known)                 │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L4  │ exdata_example.py:85                  │ Error message says sel_data instead of data_sel (already known)                   │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L5  │ mhfit_siman.py:59/67                  │ Duplicate ZETA key (already known)                                                │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L6  │ mhfit_siman.py:185/192                │ Duplicate 't' key in hf_li (already known)                                        │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L7  │ mhfit_siman.py:163–179                │ 9 dead entries in hf_pd (already known)                                           │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L8  │ mcmc_analysis.py:65                   │ Deprecated set_yticklabels([]) (already known)                                    │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L9  │ mcmc_analysis.py / spinh_example.py   │ plt.show() without non-interactive backend; blocks on headless servers            │
  ├─────┼───────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────┤
  │ L10 │ mesh_fit.py:175                       │ Writes output to git-tracked file (already known)                                 │
  └─────┴───────────────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────┘




Fitting changes: April 21. 

  Summary of what actually changes fitting behaviour:

   1. gsl_nls — NLS residuals corrected (w·(calc - obs → sqrt(w)·(calc-obs))
   2. SpinHamiltonian fits — shchisq now penalises sign differences
   3. Multi-Hamiltonian gsl_nls fits — n_rx_rt off-by-one (residuals were being mis-routed)
   4. Projection fits with hpro — uninitialised workspace fields corrected



24 April 2026

Trying to verify issues with the eryso example mesh_fit.py
There was an error in the script where it tried to add some tensors twice, so the output changed with the latest version. 
Have run it with old version on servers and it gets yet another minimum. Maybe there is some change to the minimisation. 


I still have a possible issue with home/users/mfr24/dev/pycf/examples/eryso/mesh_fit.py 
You previously pointed out that there was an error in the script, which is why the two output files
2017-05-17_eryso_site1.txt.20260421
and 
2017-05-17_eryso_site1.txt.20260424
were different. 

However, I have now run the new script using an old installation on a different machine (before any of the April 2026 modifications) and I get a different minimum. 

Can you tell me if the modifications over the past week would have changed behaviour of the minimization? 


ooking at how mesh_fit works—it's using nonlinear least squares optimization through ExData to fit energies, with a 
  SpinHamiltonian that includes zeeman, hyperfine, and quadrupole terms, all minimized via the BOBYQA algorithm with 
  separate weighting for each interaction type. The fitting process adjusts the magnetic field components along with the
   spin Hamiltonian parameters, accounting for sign conventions in both the field and chi-squared calculations.

  Now I'm thinking through which April changes impact this minimization process.

● Yes — several April changes would definitely change the minimum found by mesh_fit.py. Here are the ones that
  directly affect its numerical result:

  Changes that alter the minimization landscape

  1. MAGX / MAGY sign change (build comment — pre-audit)

   - mesh_fit.py uses t.MAGX, t.MAGY (lines 25-26, 88, 102) and fits an off-diagonal Zeeman g-tensor (shx1, shx2) with
   large off-diagonal components. Sign change in MAGX/MAGY changes the sign of cross-products like g_xy, so the chi² 
  surface is literally different. This alone moves the minimum.

  2. Conjugation before LAPACK call (build comment)

   - Changes the sign convention of eigenvectors / off-diagonal matrix elements used in SH projection.

  3. 44b0ce2 — eigenvector percentage: |z|² not |z|

   - Changes state-labeling → changes which calculated level is mapped to which experimental level in mode 'A'/'AS' 
  ExData. Different assignments → different chi².

  4. ae3eb4a — NLS double-weighting fix

   - Previously weights were applied twice (or inconsistently). The SH weights ('energy':
    0.001, 'zeeman': 12.0, 'hyperfine': 5e5, 'quadrupole': 3e5) effectively have different relative magnitudes now — 
  this directly reshapes the chi² and moves the minimum.

  5. 82ddced — SH chi² sign fix

   - Spin-Hamiltonian chi² contribution sign was wrong; fixing it changes total chi².

  6. 3e259a5 — NLS residual offset off-by-one

   - Residual indexing was shifted; every residual value changed.

  7. 05e4c1f — spinh int division for half-integer spins

   - mesh_fit.py uses S=1/2, I=7/2 (line 101). Int-division bug previously computed some spin matrices incorrectly for
   half-integer quantum numbers. This directly changes the SpinHamiltonian matrix elements.

  8. 475e9e7 — enforce unique SH interactions / ndof guard

   - Changes which interactions are enrolled and how chi² is normalized.

  9. 9e53d94 / f392fad — siman indexing / bounds

   - Only matters if you enable the basinhopping path (commented out), so not applicable to your current run.

  Bottom line

  Yes — the new minimum is expected to differ, and the new result should be considered more trustworthy than the old
  one because:

   - The MAGX/MAGY signs now match the standard convention, so the fitted g-tensor off-diagonals are now comparable to
   the published Sun et al. (PRB 77, 085124) values you're matching against.
   - The half-integer spin fix (05e4c1f) was a genuine correctness bug for I=7/2.
   - The NLS double-weighting and SH chi² sign fixes mean chi² is now the physically intended quantity.

  A legitimate sanity check: compare your new fitted g-tensor against the Sun et al. values in shx1 (line 91) and the
  Longdell/Chen values in shx2 (line 105). If the magnitudes now match more closely than before (even if signs of
  off-diagonals flip), that's confirmation the new minimum is the correct one.

  Would you like me to isolate which single commit accounts for the largest shift (by temporarily reverting them one
  at a time and re-running)?
-----------------------------------------------------------------------